<h3>Datei muss in CVS (Windows-Exel: Trennzeichen-getrennt) abgespeichert worden sein</h3>
<%= form_tag csv_upload_path, :method => 'post', :multipart => true do %>
  <p><%= label_tag 'file', 'Hier *.CSV-Datei auswählen' %>
  <%= file_field_tag 'csv_file' %></p>
  <%= submit_tag 'CSV hochladen', :name => nil %><p>
<% end %> 